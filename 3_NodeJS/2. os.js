// // OS module
// //NodeJS is mainly used to make servers, so OS module helps to get info about the OS, whether RAM is available or not, etc.
// //JS is just an interaction , actual features are implemented by these modules

let os = require("os");
// console.log(os.arch()); //architecture of the OS
// console.log(os.platform()); //platform of the OS

// //info about wifi
// console.log(os.networkInterfaces());

// //info about server load
// console.log(os.cpus());